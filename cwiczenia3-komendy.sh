git commit
git branch
git checkout
git merge
git rebase
bugFix
git checkout
HEAD
# ^ oraz ^^ aby dostać się do starszych gałęzi (rodziców)
#~(liczba) zamiast wpisywania kilku ^.
git reset
git revert
git cherry-pick
git rebase -i
git commit --amend
git tag
git describe
